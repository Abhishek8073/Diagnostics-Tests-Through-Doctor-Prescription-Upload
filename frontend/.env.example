# Copy this to .env.local for local dev overrides, or set VITE_API_URL
# directly in your hosting platform's environment variables when deployed.
# Vite only exposes variables prefixed with VITE_ to the browser.

# Leave unset locally — axiosClient.js already falls back to http://localhost:8085
VITE_API_URL=https://your-backend.onrender.com
